const Right = require('./right');
const Role = require('./role');
const Authentication = require('./authentication');
const TokenValidationResult = require('./tokenValidationResult');

module.exports = {
  Right,
  Role,
  Authentication,
  TokenValidationResult,
};
